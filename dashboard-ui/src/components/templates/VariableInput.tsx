// Stub file to maintain compatibility with existing code
// The actual variable input functionality is in InputWithVariables.tsx

import React from 'react';
import { Input } from '@/components/ui/Input';

// Placeholder component to maintain compatibility
export const VariableInput: React.FC<any> = (props) => {
  return <Input {...props} />;
};
