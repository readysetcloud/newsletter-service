<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, inial-scale=1.0">
    <title>Unsubscribed Successfully</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background: linear-gradient(135deg, #f0f4f8 0%, #d9e2ec 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
            line-height: 1.6;
        }
        .card {
            background: white;
            border-radius: 16px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            max-width: 540px;
            width: 100%;
            overflow: hidden;
        }
        .header {
            background: linear-gradient(135deg, #4a90e2 0%, #357abd 100%);
            padding: 40px 30px;
            text-align: center;
            color: white;
        }
        .icon-circle {
            width: 80px;
            height: 80px;
            background: rgba(255, 255, 255, 0.2);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 20px;
            backdrop-filter: blur(10px);
        }
        .icon { font-size: 48px; }
        .header h1 { font-size: 28px; font-weight: 600; margin-bottom: 8px; }
        .header p { font-size: 16px; opacity: 0.95; }
        .content { padding: 40px 30px; }
        .message {
            text-align: center;
            color: #4a5568;
            font-size: 15px;
            margin-bottom: 30px;
            line-height: 1.7;
        }
        .message strong { color: #2d3748; }
        .divider {
            height: 1px;
            background: linear-gradient(to right, transparent, #e2e8f0, transparent);
            margin: 30px 0;
        }
        .manual-section {
            background: #f7fafc;
            border-radius: 12px;
            padding: 24px;
            margin-top: 24px;
        }
        .manual-section h2 {
            font-size: 18px;
            color: #2d3748;
            margin-bottom: 8px;
            font-weight: 600;
        }
        .manual-section p {
            font-size: 14px;
            color: #718096;
            margin-bottom: 20px;
        }
        .form-group { margin-bottom: 16px; }
        .form-control {
            width: 100%;
            padding: 14px 16px;
            font-size: 15px;
            border: 2px solid #e2e8f0;
            border-radius: 8px;
            transition: all 0.2s;
            font-family: inherit;
        }
        .form-control:focus {
            outline: none;
            border-color: #4a90e2;
            box-shadow: 0 0 0 3px rgba(74, 144, 226, 0.1);
        }
        .btn {
            width: 100%;
            padding: 14px 24px;
            font-size: 15px;
            font-weight: 600;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            transition: all 0.2s;
            font-family: inherit;
        }
        .btn-primary {
            background: linear-gradient(135deg, #4a90e2 0%, #357abd 100%);
            color: white;
        }
        .btn-primary:hover:not(:disabled) {
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(74, 144, 226, 0.3);
        }
        .btn-primary:disabled { opacity: 0.6; cursor: not-allowed; }
        .alert {
            padding: 12px 16px;
            border-radius: 8px;
            font-size: 14px;
            margin-top: 16px;
            display: none;
            animation: slideIn 0.3s ease;
        }
        @keyframes slideIn {
            from { opacity: 0; transform: translateY(-10px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .alert-success {
            background: #f0fdf4;
            color: #166534;
            border: 1px solid #bbf7d0;
        }
        .alert-error {
            background: #fef2f2;
            color: #991b1b;
            border: 1px solid #fecaca;
        }
        .footer {
            text-align: center;
            padding: 20px 30px;
            background: #f7fafc;
            color: #718096;
            font-size: 13px;
        }
        @media (max-width: 600px) {
            body { padding: 10px; }
            .header { padding: 30px 20px; }
            .header h1 { font-size: 24px; }
            .content { padding: 30px 20px; }
            .manual-section { padding: 20px; }
        }
    </style>
</head>
<body>
    <div class="card">
        <div class="header">
            <div class="icon-circle">
                <span class="icon">✓</span>
            </div>
            <h1>You're Unsubscribed</h1>
            <p>We've removed you from our mailing list</p>
        </div>
        <div class="content">
            {{#if wasSuccessful}}
            <div class="message">
                <strong>You won't receive any more emails from us.</strong><br>
                We're sorry to see you go, but we respect your decision. If you change your mind, you can always resubscribe from our website.
            </div>
            <div class="divider"></div>
            <div class="manual-section">
                <h2>Still Getting Emails?</h2>
                <p>If you continue to receive emails after 24 hours, enter your email address below for complete removal:</p>
            {{else}}
            <div class="message">
                <strong>Having trouble with the unsubscribe link?</strong><br>
                No problem! You can unsubscribe by entering your email address below.
            </div>
            <div class="divider"></div>
            <div class="manual-section">
                <h2>Unsubscribe from Our Emails</h2>
                <p>Enter your email address below to unsubscribe from our mailing list:</p>
            {{/if}}
                <form id="manual-unsubscribe-form" onsubmit="handleManualUnsubscribe(event, '{{tenantId}}')">
                    <div class="form-group">
                        <input type="email" id="manual-email" name="email" class="form-control" placeholder="your@email.com" value="{{emailAddress}}" required aria-label="Email address">
                    </div>
                    <button type="submit" id="manual-submit-btn" class="btn btn-primary">Confirm Unsubscribe</button>
                    <div id="manual-message" class="alert"></div>
                </form>
            </div>
        </div>
        <div class="footer">
            Unsubscribe preferences updated
        </div>
    </div>
    <script>
        async function handleManualUnsubscribe(event, tenantId) {
            event.preventDefault();
            const emailInput = document.getElementById('manual-email');
            const submitBtn = document.getElementById('manual-submit-btn');
            const messageDiv = document.getElementById('manual-message');
            submitBtn.disabled = true;
            submitBtn.textContent = 'Processing...';
            messageDiv.style.display = 'none';
            messageDiv.className = 'alert';
            try {
                const baseUrl = window.location.origin + window.location.pathname.split('/').slice(0, -2).join('/');
                const response = await fetch(baseUrl + '/' + tenantId + '/unsubscribe', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ email: emailInput.value })
                });
                const data = await response.json();
                if (response.ok) {
                    messageDiv.className = 'alert alert-success';
                    messageDiv.textContent = data.message || 'Successfully unsubscribed';
                    emailInput.value = '';
                } else {
                    messageDiv.className = 'alert alert-error';
                    messageDiv.textContent = data.message || 'Failed to unsubscribe. Please try again.';
                }
            } catch (error) {
                messageDiv.className = 'alert alert-error';
                messageDiv.textContent = 'An error occurred. Please try again.';
            } finally {
                messageDiv.style.display = 'block';
                submitBtn.disabled = false;
                submitBtn.textContent = 'Confirm Unsubscribe';
            }
        }
    </script>
</body>
</html>

